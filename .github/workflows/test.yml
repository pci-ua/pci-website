name: App CI (test)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Install dependencies
      run: sudo apt update -yq && sudo apt install build-essential docker docker-compose -yq
    - name: Setup environment
      run: cp .env.example .env
    - name: Verification
      run: make check
    - name: Start app
      run: make start
    - name: Stop app
      run: make stop
